% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_r.R
\name{nutpie_sample_r}
\alias{nutpie_sample_r}
\title{Sample an arbitrary R log-density with nutpie's NUTS}
\usage{
nutpie_sample_r(
  fn = NULL,
  grad = NULL,
  value_grad = NULL,
  ndim = NULL,
  init = NULL,
  num_draws = 1000L,
  num_warmup = 1000L,
  seed = NULL,
  save_warmup = FALSE,
  max_treedepth = NULL,
  target_accept = NULL,
  expand = NULL,
  progress = interactive()
)
}
\arguments{
\item{fn}{Optional \verb{function(y)} returning the log-posterior density at the
unconstrained vector \code{y} (higher = more probable). For a TMB objective
\code{obj$fn} (a \emph{negative} log-likelihood), pass \code{function(y) -obj$fn(y)}.
Required unless \code{value_grad} is supplied.}

\item{grad}{Optional \verb{function(y)} returning the gradient of \code{fn} as a numeric
vector of length \code{ndim}. Required unless \code{value_grad} is supplied.}

\item{value_grad}{Optional combined callback \verb{function(y)} returning a numeric
vector \code{c(logp, gradient)}, length \code{ndim + 1}. This is the preferred fast
path for expensive transformed densities because it crosses into R once per
leapfrog step and can share intermediate work between value and gradient.}

\item{ndim}{Integer dimension of \code{y}. If \code{init} is supplied as a numeric
vector, \code{ndim} defaults to its length.}

\item{init}{Starting position. Either a numeric vector of length \code{ndim}, or a
\verb{function(chain_id)} returning one (called once with \code{1L}). Defaults to
\code{rnorm(ndim)}.}

\item{num_draws}{Post-warmup draws to keep. Default 1000.}

\item{num_warmup}{Warmup (tuning) draws. Must be at least 1. Default 1000.}

\item{seed}{Integer RNG seed. Default random.}

\item{save_warmup}{Whether to retain warmup draws + diagnostics
(see \code{\link[=nutpie_warmup_draws]{nutpie_warmup_draws()}}). Default \code{FALSE}.}

\item{max_treedepth}{Optional NUTS maximum tree depth.}

\item{target_accept}{Optional target acceptance probability in \verb{(0, 1)}.}

\item{expand}{A \verb{function(y)} mapping an unconstrained draw to the values you
want reported — e.g. the back-transform from a preconditioned space, or
derived quantities. Called once per kept draw (never in the leapfrog hot
loop). Should return a numeric vector; if named, the names become the
variable names. Default: report \code{y} as \verb{y1..y\{ndim\}}.}

\item{progress}{Whether to print a periodic one-line status to the console.
Defaults to \code{TRUE} in interactive sessions.}
}
\value{
A \link[posterior:draws_array]{posterior::draws_array} carrying the same diagnostics attributes
that \code{\link[=nutpie_diagnostics]{nutpie_diagnostics()}} and the posterior/bayesplot tooling consume, so
they work as usual. Diagnostics cover divergences, leapfrog count
(\code{n_steps}), step size, tree depth, energy, \code{logp}, and mean acceptance.
(Unlike \code{\link[=nutpie_sample]{nutpie_sample()}} output, no \code{sampler_config} attribute is attached.)
The \code{callback_stats} attribute reports R-callback evaluation counts and
timing, useful for separating sampler geometry from implementation overhead.
}
\description{
Run nuts-rs NUTS over a log-posterior supplied as plain R functions, instead
of a compiled Stan model. This lets you put models that already expose an R
density + gradient — e.g. a preconditioned \code{RTMB}/\code{TMB} objective — through
nutpie's adaptation, in the spirit of
\href{https://github.com/andrjohns/StanEstimators}{StanEstimators}.
}
\details{
\strong{Single chain only.} R is single-threaded, so the density callbacks must run
on the main R thread; the parallel, multi-chain machinery used by
\code{\link[=nutpie_sample]{nutpie_sample()}} is not available here. Run this several times (varying
\code{seed}) if you want multiple chains.

For expensive or preconditioned \code{RTMB}/\code{TMB} densities, prefer \code{value_grad}
over separate \code{fn}/\code{grad} callbacks. It calls back into R once per leapfrog
evaluation instead of twice, and lets you share intermediate work such as a
sparse back-transform. With \code{RTMB}/\code{TMB}, it is often fastest to compute the
gradient first and then the value at the same point, since the objective can
reuse the most recent tape evaluation.

Use \code{\link[=nutpie_diagnostics]{nutpie_diagnostics()}} for per-draw sampler diagnostics and
\code{attr(fit, "callback_stats")} to separate sampler geometry from R-callback
overhead. The latter is useful when comparing against other R-callback
samplers: \code{mean(n_steps)} describes trajectory length, while
\code{callback_seconds}, \code{logp_evals}, and \code{seconds_per_logp_eval} describe the
implementation cost of evaluating the supplied density.
}
\examples{
\dontrun{
# Split callback interface: convenient, but two R calls per leapfrog eval.
fn <- function(y) -sum(y^2) / 2
grad <- function(y) -y

fit <- nutpie_sample_r(fn, grad, ndim = 2, seed = 1)
nutpie_diagnostics(fit)
attr(fit, "callback_stats")

# Combined callback interface: preferred for expensive transformed targets.
value_grad <- function(y) {
  # For an RTMB/TMB transformed density, this is where you would compute
  # x <- solve(Lt, y)[iperm] once, then reuse x for gradient and value.
  c(fn(y), grad(y))
}

fit <- nutpie_sample_r(value_grad = value_grad, ndim = 2, seed = 1)

# Multiple chains are run as independent single-chain fits.
fits <- lapply(1:4, function(chain) {
  nutpie_sample_r(value_grad = value_grad, ndim = 2, seed = 1000 + chain)
})
draws <- posterior::bind_draws(fits, along = "chain")
posterior::summarise_draws(draws, "rhat", "ess_bulk", "ess_tail")
}
}
\seealso{
\code{\link[=nutpie_sample]{nutpie_sample()}} for Stan models.
}
