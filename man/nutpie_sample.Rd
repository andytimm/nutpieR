% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{nutpie_sample}
\alias{nutpie_sample}
\title{Sample from a Stan model using the NUTS sampler}
\usage{
nutpie_sample(
  model,
  data = NULL,
  num_draws = 1000L,
  num_warmup = 400L,
  num_chains = 4L,
  seed = NULL,
  max_treedepth = NULL,
  mindepth = NULL,
  target_accept = NULL,
  max_energy_error = NULL,
  extra_doublings = NULL,
  refresh = 100L,
  init = NULL,
  init_mean = NULL,
  save_warmup = FALSE,
  cores = NULL,
  pars = NULL,
  include = TRUE,
  store_divergences = FALSE,
  store_mass_matrix = FALSE,
  store_unconstrained = FALSE,
  store_gradient = FALSE,
  adaptation = c("diag", "low_rank", "low-rank"),
  low_rank_modified_mass_matrix = FALSE,
  mass_matrix_gamma = NULL,
  mass_matrix_eigval_cutoff = NULL
)
}
\arguments{
\item{model}{A \code{"nutpie_model"} object from \code{\link[=nutpie_compile_model]{nutpie_compile_model()}},
or a path to a compiled shared library.}

\item{data}{Model data. Can be:
\itemize{
\item A named list (will be converted to JSON via \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}})
\item A JSON string
\item A path to a \code{.json} file
\item \code{NULL} for models with no data block
}}

\item{num_draws}{Number of post-warmup draws per chain.}

\item{num_warmup}{Number of warmup (tuning) draws per chain.}

\item{num_chains}{Number of parallel chains.}

\item{seed}{Random seed for reproducibility.}

\item{max_treedepth}{Maximum tree depth for NUTS. The number of leapfrog
steps per draw is at most \code{2^max_treedepth}. Default \code{NULL} keeps the
nuts-rs default (currently 10).}

\item{mindepth}{Minimum tree depth for NUTS. The number of leapfrog steps
per draw is at least \code{2^mindepth}. Default \code{NULL} keeps the nuts-rs
default (currently 0).}

\item{target_accept}{Target acceptance probability for step size adaptation.
Default \code{NULL} keeps the nuts-rs default (currently 0.8).}

\item{max_energy_error}{Energy-error threshold above which a leapfrog step
is treated as a divergence. Default \code{NULL} keeps the nuts-rs default.}

\item{extra_doublings}{Number of additional tree doublings allowed after a
turning point is reached. Default \code{NULL} keeps the nuts-rs default
(currently 0).}

\item{refresh}{How often to print progress updates, in draws per chain.
Set to \code{0} to suppress progress output. Default is \code{100}.}

\item{init}{Initial values for each chain. Single entry point that
dispatches on the input shape:
\describe{
\item{\code{NULL} (default)}{Each chain starts from a Uniform(-2, 2) draw on
the unconstrained scale (nuts-rs default).}
\item{Scalar numeric \code{x}}{Each chain starts from a Uniform(-x, x) draw
on the unconstrained scale. \code{x = 0} starts every chain at the origin.
Must be non-negative.}
\item{Named list, e.g. \code{list(mu = 0, sigma = 1)}}{Constrained values
used to start each chain. If fully specified, every chain starts at
the same point. If partial (some parameters missing), each chain
gets its own random fill for the missing parameters (per-chain seeds
derived from \code{seed}).}
\item{List of \code{num_chains} named lists}{One constrained start per
chain. Each element may be partial.}
\item{Function \code{function(chain_id) ...}}{Called once per chain with
\code{chain_id} in \code{1:num_chains}; must return a (possibly partial)
named list of constrained parameter values.}
\item{Character path(s)}{A JSON file path, or a character vector of
\code{num_chains} JSON file paths (CmdStan-style). Parsed values are
treated as constrained named lists.}
}
No jitter is applied; starting points are used exactly (after any
random fill for partial constrained inits). To work on the unconstrained
scale, see \code{\link[=nutpie_unconstrain]{nutpie_unconstrain()}} to convert constrained values first.

\strong{Chain assignment is unspecified.} When supplying per-chain starts
(list-of-lists or \verb{function(chain_id)}), the N positions are guaranteed
to be distributed one-per-chain, but the mapping from list index /
\code{chain_id} to the output chain dimension is not currently guaranteed.
Use this to provide N dispersed starts; do not rely on "chain 1 starts
at element 1" for downstream identifiability. (Threading the true
chain id requires an upstream change in nuts-rs; tracked for a future
release.)}

\item{init_mean}{Deprecated. Scalar or numeric vector on the unconstrained
scale, with ±0.5 uniform jitter per chain. Use
\code{init = function(chain_id) ...}, \verb{init = <scalar>} for Uniform(-x, x), or
\verb{init = <named list>} instead. Will be removed in a future version.}

\item{save_warmup}{If \code{TRUE}, warmup draws and diagnostics are attached as
attributes. Retrieve them with \code{\link[=nutpie_warmup_draws]{nutpie_warmup_draws()}}.}

\item{cores}{Number of CPU cores to use for parallel sampling. Defaults to
\code{min(num_chains, parallel::detectCores())}.}

\item{pars}{An optional character vector of block-level parameter names
(e.g. \code{"beta"}, \code{"sigma"}). When supplied, only these parameters (or
all parameters \emph{except} these, depending on \code{include}) are returned in the
output draws. By default (\code{NULL}), all parameters are returned. Parameter
names should be the Stan block names, not indexed names — e.g. \code{"beta"}
will match \code{beta}, \code{beta[1]}, \code{beta[2]}, etc. When the kept set excludes
the entire transformed-parameter and/or generated-quantities blocks,
those slices are skipped at sample time (see \code{NEWS.md} for benchmarks).}

\item{include}{Logical (default \code{TRUE}). If \code{TRUE}, \code{pars} specifies the
parameters to \emph{keep} (whitelist). If \code{FALSE}, \code{pars} specifies parameters
to \emph{exclude} (blacklist). Ignored when \code{pars} is \code{NULL}.}

\item{store_divergences}{If \code{TRUE}, store detailed information about each
divergent transition (start/end positions, momentum, gradient). Surfaces
as list columns on diagnostics, only present when at least one draw
diverged. Default \code{FALSE}.}

\item{store_mass_matrix}{If \code{TRUE}, store the inverse mass matrix diagonal
at each draw. Surfaces as a numeric matrix
\verb{(n_draws * n_chains, ndim_unc)} when uniform-width; falls back to a
list of vectors when widths differ. Default \code{FALSE}.}

\item{store_unconstrained}{If \code{TRUE}, store the unconstrained position at
each draw on diagnostics (\code{unconstrained_draw}). Surfaces as a numeric
matrix \verb{(n_draws * n_chains, ndim_unc)} when uniform-width; falls back
to a list of vectors when widths differ. For high-dimensional models
this can rival the draws matrix in size. Default \code{FALSE}.}

\item{store_gradient}{If \code{TRUE}, store the log-density gradient at each
draw on diagnostics (\code{gradient}). Same shape profile as
\code{store_unconstrained}. Default \code{FALSE}.}

\item{adaptation}{Mass matrix adaptation strategy. One of:
\describe{
\item{\code{"diag"} (default)}{Diagonal mass matrix (the standard NUTS
choice).}
\item{\code{"low_rank"} / \code{"low-rank"}}{Low-rank modified mass matrix
adaptation. Captures posterior correlations and can improve
efficiency on models with strongly correlated parameters.}
}
Matches the Python nutpie \verb{adaptation=} API. Additional strategies
(\code{"draw_diag"}, \code{"flow"}) may be added in future releases.}

\item{low_rank_modified_mass_matrix}{Deprecated. If \code{TRUE}, equivalent to
\code{adaptation = "low_rank"}. Will be removed in a future release.}

\item{mass_matrix_gamma}{Regularisation parameter for low-rank mass matrix
adaptation. Only used when \code{adaptation = "low_rank"}; ignored otherwise.
Default \code{NULL} keeps the nuts-rs default (currently \code{1e-5}).}

\item{mass_matrix_eigval_cutoff}{Eigenvalue cutoff for low-rank mass matrix.
Eigenvalues outside \verb{(1/cutoff, cutoff)} are ignored. Only used when
\code{adaptation = "low_rank"}; ignored otherwise. Default \code{NULL} keeps the
nuts-rs default (currently \code{2.0}).}
}
\value{
A \code{posterior::draws_array} with dimensions
\verb{(num_draws, num_chains, n_params)}. Sampler diagnostics are attached
as an attribute and can be retrieved with \code{\link[=nutpie_diagnostics]{nutpie_diagnostics()}}; see
\code{?nutpie_diagnostics} for the chain / draw indexing convention (1-indexed,
phase-relative). The attributes \code{"num_warmup"} and \code{"num_draws"} record
the sampling configuration (accessible via \code{attr(draws, "num_warmup")}
etc.). The \code{"sampler_config"} attribute is a JSON string capturing the
\emph{effective} \code{nuts-rs} settings used (including any defaults that were
left unspecified by the caller, and exposing \code{num_warmup} to match the
function argument name); parse with \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}}.
}
\description{
Runs the nuts-rs NUTS sampler on a compiled Stan model.
}
