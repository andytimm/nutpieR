% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{nutpie_sample}
\alias{nutpie_sample}
\title{Sample from a Stan model using the NUTS sampler}
\usage{
nutpie_sample(
  model,
  data = NULL,
  num_draws = 1000L,
  num_warmup = 400L,
  num_chains = 4L,
  seed = NULL,
  max_treedepth = 10L,
  target_accept = 0.8,
  refresh = 100L,
  init = NULL,
  init_mean = NULL,
  save_warmup = FALSE,
  cores = NULL,
  pars = NULL,
  include = TRUE,
  store_divergences = FALSE,
  store_mass_matrix = FALSE,
  low_rank_modified_mass_matrix = FALSE,
  mass_matrix_gamma = 1e-05,
  mass_matrix_eigval_cutoff = 2
)
}
\arguments{
\item{model}{A \code{"nutpie_model"} object from \code{\link[=nutpie_compile_model]{nutpie_compile_model()}},
or a path to a compiled shared library.}

\item{data}{Model data. Can be:
\itemize{
\item A named list (will be converted to JSON via \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}})
\item A JSON string
\item A path to a \code{.json} file
\item \code{NULL} for models with no data block
}}

\item{num_draws}{Number of post-warmup draws per chain.}

\item{num_warmup}{Number of warmup (tuning) draws per chain.}

\item{num_chains}{Number of parallel chains.}

\item{seed}{Random seed for reproducibility.}

\item{max_treedepth}{Maximum tree depth for NUTS. The number of leapfrog
steps per draw is at most \code{2^max_treedepth}.}

\item{target_accept}{Target acceptance probability for step size adaptation.}

\item{refresh}{How often to print progress updates, in draws per chain.
Set to \code{0} to suppress progress output. Default is \code{100}.}

\item{init}{Initial values for each chain. Single entry point that
dispatches on the input shape:
\describe{
\item{\code{NULL} (default)}{Each chain starts from a Uniform(-2, 2) draw on
the unconstrained scale (nuts-rs default).}
\item{Scalar numeric \code{x}}{Each chain starts from a Uniform(-x, x) draw
on the unconstrained scale. \code{x = 0} starts every chain at the origin.
Must be non-negative.}
\item{Named list, e.g. \code{list(mu = 0, sigma = 1)}}{Broadcast constrained
values to all chains. Partial inits are allowed — missing parameters
are filled with random draws (per chain, seeded from \code{seed}).}
\item{List of \code{num_chains} named lists}{Per-chain constrained inits
(different starting point per chain). Each element may be partial.}
\item{Function \code{function(chain_id) ...}}{Called once per chain with
\code{chain_id} in \code{1:num_chains}; must return a (possibly partial)
named list of constrained parameter values.}
\item{Character path(s)}{A JSON file path, or a character vector of
\code{num_chains} JSON file paths (CmdStan-style). Parsed values are
treated as constrained named lists.}
}
No jitter is applied; starting points are used exactly (after any
random fill for partial constrained inits). To work on the unconstrained
scale, see \code{\link[=nutpie_unconstrain]{nutpie_unconstrain()}} to convert constrained values first.}

\item{init_mean}{Deprecated. Scalar or numeric vector on the unconstrained
scale, with ±0.5 uniform jitter per chain. Use
\code{init = function(chain_id) ...}, \code{init = <scalar>} for Uniform(-x, x), or
\code{init = <named list>} instead. Will be removed in a future version.}

\item{save_warmup}{If \code{TRUE}, warmup draws and diagnostics are attached as
attributes. Retrieve them with \code{\link[=nutpie_warmup_draws]{nutpie_warmup_draws()}}.}

\item{cores}{Number of CPU cores to use for parallel sampling. Defaults to
\code{min(num_chains, parallel::detectCores())}.}

\item{pars}{An optional character vector of block-level parameter names
(e.g. \code{"beta"}, \code{"sigma"}). When supplied, only these parameters (or
all parameters \emph{except} these, depending on \code{include}) are returned in the
output draws. By default (\code{NULL}), all parameters are returned. Parameter
names should be the Stan block names, not indexed names — e.g. \code{"beta"}
will match \code{beta}, \code{beta[1]}, \code{beta[2]}, etc.}

\item{include}{Logical (default \code{TRUE}). If \code{TRUE}, \code{pars} specifies the
parameters to \emph{keep} (whitelist). If \code{FALSE}, \code{pars} specifies parameters
to \emph{exclude} (blacklist). Ignored when \code{pars} is \code{NULL}.}

\item{store_divergences}{If \code{TRUE}, store detailed information about each
divergent transition (start/end positions, momentum, gradient). Adds
list columns to diagnostics.}

\item{store_mass_matrix}{If \code{TRUE}, store the inverse mass matrix diagonal
at each draw. Adds a list column to diagnostics.}

\item{low_rank_modified_mass_matrix}{If \code{TRUE}, use low-rank modified mass
matrix adaptation. This can improve sampling efficiency for models with
correlated parameters by capturing posterior correlations in the mass
matrix. Default is \code{FALSE} (diagonal mass matrix).}

\item{mass_matrix_gamma}{Regularisation parameter for low-rank mass matrix
adaptation. Only used when \code{low_rank_modified_mass_matrix = TRUE}.
Default is \code{1e-5}.}

\item{mass_matrix_eigval_cutoff}{Eigenvalue cutoff for low-rank mass matrix.
Eigenvalues outside \verb{(1/cutoff, cutoff)} are ignored. Only used when
\code{low_rank_modified_mass_matrix = TRUE}. Default is \code{2.0}.}
}
\value{
A \code{posterior::draws_array} with dimensions
\verb{(num_draws, num_chains, n_params)}. Sampler diagnostics are attached
as an attribute and can be retrieved with \code{\link[=nutpie_diagnostics]{nutpie_diagnostics()}}.
The attributes \code{"num_warmup"} and \code{"num_draws"} record the sampling
configuration (accessible via \code{attr(draws, "num_warmup")} etc.).
}
\description{
Runs the nuts-rs NUTS sampler on a compiled Stan model.
}
